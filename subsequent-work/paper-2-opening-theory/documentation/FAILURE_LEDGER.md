# Failure ledger

## Resolved during integration

1. **Source duplication:** 631 duplicate-content groups could have inflated apparent evidence. Resolved by SHA-256 deduplication and representative scanning.
2. **Filename authority:** parenthetical copy numbers differed from prompt expectations. Resolved by byte hashes and correction history.
3. **ARC1 field scope:** broad prose risked exporting a real-Hermitian conclusion to complex constants. Resolved by the colleague clarification.
4. **PB1 identity:** an unconditional initial-form equality was false. Replaced by valuation inequality and non-vanishing criterion.
5. **Fixed-weight scope:** “no global finite atlas” had been read too broadly. Resolved by the fixed-weight Noetherian theorem.
6. **TFP label:** no authoritative source. Recorded `UNAVAILABLE`; TF1 retained.
7. **TeX path initialisation:** the frozen TinyTeX runtime failed from a Windows path containing spaces. Resolved by staging the same runtime at `C:\m3aii_tinytex` for build verification.

## Deliberate falsifier

The controlled edge-sign perturbation is expected to disagree with the locked theorem. It is labelled `FALSIFIED` and is a successful diagnostic outcome, not a release failure.

## Open failures

None affecting the stated theorems or current author-review PDF. Remaining research limitations are in `UNRESOLVED_GAPS.md`.

## Fresh-extraction launcher correction

The first archive test on 3 August 2026 was `FALSIFIED` at the launcher layer: invoking `REPRODUCE.ps1` from outside the extracted release made its relative `CODE/` paths resolve against the caller's directory. No mathematical check ran. Both launchers were changed to enter their own release root before verification, the manifest and archive were regenerated, and a second untouched extraction was required before Gate 10 could close.
