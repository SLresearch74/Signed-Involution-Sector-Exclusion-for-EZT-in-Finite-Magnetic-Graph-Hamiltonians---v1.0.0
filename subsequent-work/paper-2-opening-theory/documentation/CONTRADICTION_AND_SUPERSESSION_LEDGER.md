# Contradiction and supersession ledger

## Resolved contradictions

1. **ARC1 real cancellation language.** Earlier summaries could be read as ruling out cancellation for all real parameter paths. The corrected result is conditional: the family must be real-analytic Hermitian and the relevant leading constants real and non-zero. Over the complex torus the tie cancels at the explicit value in equation (9.3). The colleague clarification supersedes broader prose.

2. **Fixed-weight termination.** MV1 correctly showed that no weight-only global recursive atlas or uniform truncation depth had been established. It was too strong to read this as failure of finite termination after a weight is fixed. Polynomial/Laurent coefficient rings are Noetherian, which proves a finite coefficient-stratified decision for each fixed positive rational weight. The uniform/global negative result remains.

3. **PB1 initial-form identity.** The literal identity between the child initial form and the graded pullback of the parent initial form fails when the candidate face cancels. The corrected theorem is a valuation inequality plus equality if and only if the candidate component is non-zero.

4. **TF1 historical readiness tokens.** Earlier assessment vocabulary is not scientific evidence and is not imported. The present release uses only the locked evidence labels. The R1.2 mathematics, not historical workflow flags, controls the paper.

5. **TFP label discrepancy.** No authoritative scientific source used `TFP`. The integrated paper uses `TF1` only and records `TFP` as `UNAVAILABLE`.

## Non-contradictions clarified

- The numerator has a cubic displayed term, yet no cubic fixed-ray branch: the syzygy places the cubic coefficient in `(L,Q)`.
- Moment and amplitude supports coincide, yet their pulled face zeros can differ: multiplication by `(-i)^k/k!` preserves non-zero support but changes relative coefficients.
- A finite decision exists at every fixed weight, yet there is no finite global atlas: the finite generating set and its maximum index may depend non-effectively on the weight.
- The dark ideal is reduced, yet four initial ideals are non-radical: initial degeneration need not preserve radicality.

