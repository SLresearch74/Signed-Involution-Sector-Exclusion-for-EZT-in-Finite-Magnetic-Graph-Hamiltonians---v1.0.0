# Corrected T16 freeze — prerequisite to T17

## A. Response coefficient field

Let K be a fixed coefficient field containing the marked cyclotomic Song field, the baseline spectral poles, and all fixed declared amplitudes. Keep the distinguished subfield K_Song=Q(zeta_n) explicit and use R=K[t]. For an oriented Hermitian circulant, Q(zeta_n,i), enlarged by fixed amplitudes when necessary, is a safe choice. At n=3, i(zeta_3-zeta_3^(-1))=-sqrt(3) is not in Q(sqrt(-3)); this shows why K_Song alone is insufficient. For the locked n=8 family K=K_Song=Q(i,sqrt(2)), so its arithmetic is unchanged.

## B. Locally closed strata

Use Sigma=V(I_Sigma) intersect D(S), A_Sigma=S^(-1)(R/I_Sigma), J_(gamma,Sigma)=J_gamma A_Sigma, and contraction (J_gamma+I_Sigma):S^infinity. Apply the same convention to edge ideals. A rebuilt model on a rank-collapse stratum has its own block inventory; extending an ambient ideal does not create a physical block that has disappeared.

The locked n=8 nonzero stratum has I_Sigma=0, S={t^m:m>=0}, and A_Sigma=K8[t,t^(-1)]. Its T16 saturation formula remains valid. A quotient stratum need not be a PID domain; assertions about monic polynomial generators apply only to the specified PID strata. On other strata retain ideals in A_Sigma. The zero ring/empty stratum is excluded.

## C. Zero-generator normalization

Set g=0 for the zero ideal. For a nonzero ideal on a one-parameter PID stratum, remove precisely the inverted factors and choose the monic associate in the marked coordinate. Set g=1 for the unit ideal. Use the same rule for h. A nonconstant generator describes a proper geometric locus only after restricting to points of Sigma; a root need not belong to the base field K. No monic generator is assigned to the zero ideal.

## Standing and unchanged regression targets

T16-A through T16-E: PROVED-SYNTHESIS under the repaired hypotheses. Actual n=8 T16-C/F instances: VERIFIED-EXACT. Content, gcd, localization, coefficient fields and ideal base change: PRIOR-ART-OWNED. General compact arithmetic classification and novelty: OPEN.

Required unchanged targets: 16 components; 112 block ideals with raw generators 1 x 48, t x 56, 0 x 8; 204 edges with raw generators 1 x 140, t x 64; on D(t), 104 unit blocks and eight zero block-4 generators, and 204 unit edge generators. All locked K0/Kmax comparisons must pass. The factors 2t-i and 2t+i occur in no normalized block or edge generator. The T17 computation must run the inherited verifier and independently compare these generator and closure records before beginning classification; its evidence is recorded in the T17 machine ledger.
